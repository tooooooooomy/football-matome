language: rust
rust:
    - stable

before_script:
    - "export PATH=$HOME/.meteor:$PATH"

cache: yarn

script:
    - make travis
    - make test
    - cd ./client/ && npm install -g yarn && make
    - make test

git:
    submodules: false
